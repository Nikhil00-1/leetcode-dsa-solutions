class Solution:
    def minCost(self, height):
        
        n=len(height)
        
        dp=[0]*n
        
        for j in range(1,n):
            dp[j]=min(dp[i]+abs(height[j]-height[i]) for i in [j-1,j-2] if i>=0)
        return dp[n-1]
