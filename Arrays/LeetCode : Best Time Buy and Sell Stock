class Solution:
    def maxProfit(self, prices: List[int]) -> int:

        n=len(prices)
        mx=0
        mn=float('inf')
        for i in range(n):
            if prices[i]-mn>mx:
                mx=prices[i]-mn
            if mn>prices[i]:
                mn=prices[i]
        return mx
